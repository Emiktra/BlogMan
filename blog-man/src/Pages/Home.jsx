import React, {useContext} from 'react'
import AuthContext from '../Contexts/AuthContext'

export const Home = () => {
    const {user, userValue} = useContext(AuthContext)

    

    return (
        <div>
            
        </div>
    )
}
